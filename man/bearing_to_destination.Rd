% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bearings.R
\name{bearing_to_destination}
\alias{bearing_to_destination}
\title{Compute the bearing from each mosque to a single destination}
\usage{
bearing_to_destination(
  data,
  destination,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude"
)
}
\arguments{
\item{data}{A data frame with columns \code{lat_col} and \code{lon_col} (typically
\link{gibson_qibla} or a subset of it).}

\item{destination}{A single-row data frame, or a named list/vector, with
fields \code{latitude} and \code{longitude} (or the names supplied to \code{lat_col}
and \code{lon_col}). Typically one row of \link{destinations}.}

\item{model}{A bearing model function with signature
\code{f(from_lat, from_lon, to_lat, to_lon)}. Default: \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col}{Column names for latitude and longitude in \code{data}
(and \code{destination} if it is a data frame). Defaults: \code{"latitude"},
\code{"longitude"}.}
}
\value{
Numeric vector, one value per row of \code{data}, in [0, 360).
}
\description{
Adds a bearing (in degrees clockwise from north) from every row in \code{data}
to a specified destination point. The bearing model is replaceable; see
\code{\link[=bearing_haversine]{bearing_haversine()}}, \code{\link[=bearing_rhumb]{bearing_rhumb()}}, \code{\link[=bearing_vincenty]{bearing_vincenty()}}.
}
\examples{
data(gibson_qibla)
data(destinations)
mecca <- destinations[destinations$id == "mecca", ]
head(bearing_to_destination(gibson_qibla, mecca))
}
\seealso{
\code{\link[=bearing_to_candidates]{bearing_to_candidates()}}, \code{\link[=angular_difference]{angular_difference()}},
\code{\link[=signed_angular_error]{signed_angular_error()}}
}
