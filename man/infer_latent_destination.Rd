% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intermediate.R
\name{infer_latent_destination}
\alias{infer_latent_destination}
\title{Infer the implied latent destination from mosque orientations}
\usage{
infer_latent_destination(
  data,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude",
  azimuth_col = "azimuth",
  init_lat = 25,
  init_lon = 40
)
}
\arguments{
\item{data}{A mosque data frame with azimuth measurements.}

\item{model}{Bearing model function. Default \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col, azimuth_col}{Column names in \code{data}.}

\item{init_lat, init_lon}{Starting coordinates for the optimisation (decimal
degrees). Default: 25 N, 40 E (central Middle East).}
}
\value{
A named list:
\describe{
\item{\code{latitude}, \code{longitude}}{Inferred destination (decimal degrees).}
\item{\code{mean_absolute_error}}{Mean absolute angular error at the solution (degrees).}
\item{\code{n_used}}{Number of mosques used (those with non-\code{NA} azimuths).}
\item{\code{convergence}}{Integer code from \code{\link[stats:optim]{stats::optim()}}. 0 = converged.}
}
}
\description{
Uses numerical optimisation (L-BFGS-B) to find the latitude and longitude
of the point that minimises the mean absolute angular error between observed
mosque azimuths and the theoretical bearing from each mosque to that point.
This is a data-driven alternative to specifying a destination a priori.
}
\details{
The result is a statistical estimate, not a historical identification. It
should be reported as an analytical tool and not interpreted as evidence for
any particular destination (package neutrality principle).
}
\examples{
data(gibson_qibla)
res <- infer_latent_destination(gibson_qibla)
cat("Inferred lat:", round(res$latitude, 2), "\n")
cat("Inferred lon:", round(res$longitude, 2), "\n")
cat("Mean error:  ", round(res$mean_absolute_error, 2), "deg\n")
}
\seealso{
\code{\link[=intermediate_bearing]{intermediate_bearing()}}
}
