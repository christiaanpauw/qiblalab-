% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bearings.R
\name{bearing_to_candidates}
\alias{bearing_to_candidates}
\title{Compute bearings from each mosque to all candidate destinations}
\usage{
bearing_to_candidates(
  data,
  candidates = NULL,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude",
  data_id_col = "row_id"
)
}
\arguments{
\item{data}{A data frame with columns \code{lat_col} and \code{lon_col} (typically
\link{gibson_qibla} or a subset of it).}

\item{candidates}{A data frame of candidate destinations with at minimum
an \code{id} column and the columns named by \code{lat_col} and \code{lon_col}.
Defaults to the shipped \link{destinations} table.}

\item{model}{A bearing model function with signature
\code{f(from_lat, from_lon, to_lat, to_lon)}. Default: \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col}{Column names for latitude and longitude in \code{data}
(and \code{destination} if it is a data frame). Defaults: \code{"latitude"},
\code{"longitude"}.}

\item{data_id_col}{Column in \code{data} used to identify each site in the
output. Defaults to \code{"row_id"}.}
}
\value{
A tibble with columns: the \code{data_id_col} from \code{data}, \code{destination_id}
(from \code{candidates$id}), \code{destination_name} (from \code{candidates$name} if
present), and \code{bearing}.
}
\description{
For each row in \code{data}, computes the bearing to every row in \code{candidates}
and returns a long-format tibble. This is the primary entry point for
multi-destination comparison.
}
\examples{
data(gibson_qibla)
data(destinations)
bearings <- bearing_to_candidates(gibson_qibla)
head(bearings)
}
\seealso{
\code{\link[=bearing_to_destination]{bearing_to_destination()}}, \code{\link[=angular_difference]{angular_difference()}}
}
