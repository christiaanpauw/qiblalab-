% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intermediate.R
\name{intermediate_bearing}
\alias{intermediate_bearing}
\title{Compute an implied intermediate qibla bearing}
\usage{
intermediate_bearing(
  data,
  method = c("circular_midpoint", "weighted", "free_latent", "common_convention",
    "latent_cluster"),
  dest1 = NULL,
  dest2 = NULL,
  weight = 0.5,
  convention = NULL,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude",
  azimuth_col = "azimuth",
  dest_table = NULL
)
}
\arguments{
\item{data}{A mosque data frame (e.g. \link{gibson_qibla}).}

\item{method}{One of the five method strings (see Details).}

\item{dest1, dest2}{Destination: a character id from \link{destinations} or a
single-row data frame with \code{latitude} and \code{longitude}. Required for
\code{"circular_midpoint"} and \code{"weighted"}.}

\item{weight}{Numeric in [0, 1]. Weight on the bearing to \code{dest1} for the
\code{"weighted"} method. Default \code{0.5}.}

\item{convention}{Character scalar. Named convention for
\code{"common_convention"}. One of \code{"due_south"}, \code{"due_north"},
\code{"solar_east"}, \code{"solar_west"}.}

\item{model}{Bearing model function. Default \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col, azimuth_col}{Column names in \code{data}.}

\item{dest_table}{Destinations table used to resolve character ids. Defaults
to the shipped \link{destinations}.}
}
\value{
Numeric vector in [0, 360), one value per row of \code{data}. \code{NA} where
a required bearing or azimuth is \code{NA}.
}
\description{
Returns one implied qibla bearing per mosque using one of five methods.
The "intermediate" concept acknowledges that early mosques may not have been
oriented toward any single modern reference point.
}
\details{
Methods:
\describe{
\item{\code{circular_midpoint}}{Circular mean of the bearings from each mosque
to \code{dest1} and \code{dest2}. Requires both \code{dest1} and \code{dest2}.}
\item{\code{weighted}}{Weighted circular mean: \code{weight} on the bearing to
\code{dest1}, \code{1 - weight} on the bearing to \code{dest2}. At \code{weight = 0.5}
this equals \code{circular_midpoint}. Requires both \code{dest1} and \code{dest2}.}
\item{\code{free_latent}}{Calls \code{\link[=infer_latent_destination]{infer_latent_destination()}} to find the
geographic point that minimises mean absolute angular error across all
mosques with measured azimuths, then returns the bearing from each
mosque to that point. Requires \code{azimuth_col} to be present.}
\item{\code{common_convention}}{Returns a fixed bearing based on a named
historical convention. Requires \code{convention}. Currently supported:
\code{"due_south"} (180), \code{"due_north"} (0), \code{"solar_east"} (90),
\code{"solar_west"} (270).}
\item{\code{latent_cluster}}{Returns the circular mean of all observed
azimuths — the global consensus direction implied by the dataset.
This is a one-cluster approximation; proper clustering is in Phase 5.}
}
}
\examples{
data(gibson_qibla)
data(destinations)
mecca <- destinations[destinations$id == "mecca", ]
petra <- destinations[destinations$id == "petra", ]
mid   <- intermediate_bearing(gibson_qibla, dest1 = mecca, dest2 = petra)
head(mid)

wt  <- intermediate_bearing(gibson_qibla, method = "weighted",
                            dest1 = "mecca", dest2 = "petra", weight = 0.7)
head(wt)
}
\seealso{
\code{\link[=infer_latent_destination]{infer_latent_destination()}}, \code{\link[=bearing_to_destination]{bearing_to_destination()}}
}
