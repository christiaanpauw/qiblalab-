% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_qibla_data}
\alias{prepare_qibla_data}
\title{Prepare raw Qibla data for analysis}
\usage{
prepare_qibla_data(raw)
}
\arguments{
\item{raw}{A data frame returned by \code{\link[=import_qibla_data]{import_qibla_data()}}.}
}
\value{
A tibble with 17 columns (see \link{gibson_qibla} for full documentation).
Carries \code{provenance}, \code{audit_log}, and \code{missing_fields} attributes.
}
\description{
Applies the documented cleaning pipeline to data returned by
\code{\link[=import_qibla_data]{import_qibla_data()}}, producing the same form as the shipped
\link{gibson_qibla} dataset. Every action is recorded in an audit log attached
to the result as \code{attr(., "audit_log")}.
}
\details{
Permitted operations (see ADR-06):
\itemize{
\item Column renaming to snake_case (original names retained in source columns)
\item Case standardisation of categorical values
\item Spelling corrections in \code{Country} (originals retained in \code{country_source})
\item Parsing of \verb{Year CE} strings into \code{year_ce_min} / \code{year_ce_max} integers
}

Not done here (deliberate — see ADRs):
\itemize{
\item Dropping or imputing records
\item Parsing \code{Rebuilt} free text
\item Any bearing or orientation calculation
}
}
\examples{
\dontrun{
raw     <- import_qibla_data()
cleaned <- prepare_qibla_data(raw)
attr(cleaned, "audit_log")
}
}
\seealso{
\code{\link[=import_qibla_data]{import_qibla_data()}}, \code{\link[=validate_qibla_data]{validate_qibla_data()}}, \link{gibson_qibla}
}
