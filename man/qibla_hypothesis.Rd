% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{qibla_hypothesis}
\alias{qibla_hypothesis}
\title{Specify a qibla orientation hypothesis}
\usage{
qibla_hypothesis(
  name,
  population = TRUE,
  candidate,
  tolerance,
  expected_share = 0.5,
  alternative = c("greater", "two.sided", "less"),
  description = NULL,
  comments = NULL
)
}
\arguments{
\item{name}{Character scalar. Short identifier for the hypothesis
(used in output labels).}

\item{population}{An unquoted filter expression evaluated against the
mosque data frame (e.g. \code{year_ce_max <= 700}). Default \code{TRUE} (all rows).}

\item{candidate}{Character scalar (id in \link{destinations}) or single-row
data frame with \code{latitude} and \code{longitude}. The proposed qibla destination.}

\item{tolerance}{Numeric scalar in (0, 180]. Angular tolerance in degrees.
Required: there is no default (ADR-08).}

\item{expected_share}{Numeric in (0, 1). Proportion of mosques expected to
be consistent with the candidate under H0. Default \code{0.5}.}

\item{alternative}{\code{"greater"}, \code{"two.sided"}, or \code{"less"}. Direction of
the proportion test H1. Default \code{"greater"}.}

\item{description}{Character scalar. Optional longer description of the
research question.}

\item{comments}{Character scalar. Optional analyst notes (methodology,
caveats, preregistration details).}
}
\value{
A \code{qibla_hypothesis} object (list).
}
\description{
Creates a structured hypothesis object recording all analytical choices
before any data are examined. The hypothesis can then be tested with
\code{\link[=test_qibla_hypothesis]{test_qibla_hypothesis()}}.
}
\examples{
h <- qibla_hypothesis(
  name           = "Early Petra orientation",
  population     = year_ce_max <= 700,
  candidate      = "petra",
  tolerance      = 5,
  expected_share = 0.5,
  alternative    = "greater",
  description    = "Most mosques built before 700 CE pointed toward Petra"
)
h
}
\seealso{
\code{\link[=test_qibla_hypothesis]{test_qibla_hypothesis()}}, \code{\link[=compare_qibla_hypotheses]{compare_qibla_hypotheses()}}
}
