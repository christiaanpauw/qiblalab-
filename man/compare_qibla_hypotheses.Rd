% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{compare_qibla_hypotheses}
\alias{compare_qibla_hypotheses}
\title{Compare mosque orientations against all candidate destinations}
\usage{
compare_qibla_hypotheses(
  data,
  candidates = NULL,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude",
  azimuth_col = "azimuth",
  ambiguity_threshold = 5
)
}
\arguments{
\item{data}{A mosque data frame (typically \link{gibson_qibla}).}

\item{candidates}{A destinations data frame. Defaults to \link{destinations}.}

\item{model}{Bearing model function. Default \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col, azimuth_col}{Column names in \code{data}.}

\item{ambiguity_threshold}{Numeric. If the margin between nearest and second-
nearest candidate errors is below this threshold (degrees), the mosque is
flagged as \code{ambiguous}. Default \code{5}.}
}
\value{
A tibble with columns from \code{data} plus:
\describe{
\item{bearing_{id}}{Theoretical bearing to each candidate.}
\item{error_{id}}{Absolute angular error for each candidate.}
\item{nearest_id}{Id of the closest candidate.}
\item{nearest_error}{Absolute angular error to nearest candidate.}
\item{second_nearest_id}{Id of the second-closest candidate.}
\item{second_nearest_error}{Error to second-closest.}
\item{margin}{\code{second_nearest_error - nearest_error}. Small margin = ambiguous.}
\item{ambiguous}{Logical. \code{TRUE} when \code{margin < ambiguity_threshold}.}
}
Mosques with no measured azimuth are retained with \code{NA} in all computed
columns.
}
\description{
For each mosque with a measured azimuth, computes the absolute angular error
to every candidate destination, identifies the nearest candidate, and
computes the margin over the second-nearest. A small margin flags mosques
where two candidates are practically indistinguishable (ADR-08).
}
\details{
Returns a tibble with one row per mosque, suitable for further filtering and
analysis. Bearings are also attached as columns \verb{bearing_\{id\}} and errors
as \verb{error_\{id\}} for each candidate.
}
\examples{
data(gibson_qibla)
cmp <- compare_qibla_hypotheses(gibson_qibla)
dplyr::count(cmp, nearest_id)
}
\seealso{
\code{\link[=test_qibla_hypothesis]{test_qibla_hypothesis()}}, \code{\link[=qibla_hypothesis]{qibla_hypothesis()}}
}
