% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{map_clusters}
\alias{map_clusters}
\title{Map of cluster assignments}
\usage{
map_clusters(
  cluster_result,
  lat_col = "latitude",
  lon_col = "longitude",
  size = 1.5,
  alpha = 0.7,
  title = NULL
)
}
\arguments{
\item{cluster_result}{A \code{qibla_cluster_result} from \code{\link[=cluster_qiblas]{cluster_qiblas()}}.}

\item{lat_col, lon_col}{Latitude/longitude column names in the embedded data
frame. Default \code{"latitude"} / \code{"longitude"}.}

\item{size}{Point size. Default \code{1.5}.}

\item{alpha}{Point transparency. Default \code{0.7}.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots mosque locations coloured by cluster assignment from \code{\link[=cluster_qiblas]{cluster_qiblas()}}.
Mosques without an assigned cluster are plotted in grey.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data(qibla_scenarios)
  res <- cluster_qiblas(qibla_scenarios$two_traditions, k = 2, seed = 1)
  map_clusters(res)
}
}
\seealso{
\code{\link[=cluster_qiblas]{cluster_qiblas()}}, \code{\link[=map_mosques]{map_mosques()}}
}
