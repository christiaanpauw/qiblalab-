% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{test_qibla_hypothesis}
\alias{test_qibla_hypothesis}
\title{Test a qibla orientation hypothesis against mosque data}
\usage{
test_qibla_hypothesis(
  hypothesis,
  data,
  dest_table = NULL,
  model = bearing_haversine,
  lat_col = "latitude",
  lon_col = "longitude",
  azimuth_col = "azimuth",
  sensitivity_tolerances = c(2, 5, 10)
)
}
\arguments{
\item{hypothesis}{A \code{qibla_hypothesis} object from \code{\link[=qibla_hypothesis]{qibla_hypothesis()}}.}

\item{data}{A mosque data frame (typically \link{gibson_qibla}).}

\item{dest_table}{A destinations data frame for resolving character
candidate ids. Defaults to the shipped \link{destinations}.}

\item{model}{Bearing model function. Default \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{lat_col, lon_col, azimuth_col}{Column names in \code{data}.}

\item{sensitivity_tolerances}{Numeric vector of additional tolerances to
include in the sensitivity table. Default \code{c(2, 5, 10)}.}
}
\value{
A \code{qibla_test_result} object. Print it for a human-readable report.
Key fields: \verb{$proportion_observed}, \verb{$binom_test}, \verb{$circular_summary},
\verb{$sensitivity}, \verb{$consistent} (logical vector over analysed mosques).
}
\description{
Applies a \code{\link[=qibla_hypothesis]{qibla_hypothesis()}} to a mosque data frame. For each mosque in
the specified population with a measured azimuth, it computes the bearing to
the candidate destination and classifies the mosque as "consistent" if the
absolute angular error is within the stated tolerance. A binomial test
evaluates whether the observed proportion of consistent mosques exceeds the
null share.
}
\details{
Results at the specified tolerance are always accompanied by a sensitivity
table showing how conclusions change at t = 2, 5, and 10 degrees (ADR-08).
}
\examples{
data(gibson_qibla)
h <- qibla_hypothesis(
  name      = "Early Petra orientation",
  population = year_ce_max <= 700,
  candidate = "petra",
  tolerance = 5
)
test_qibla_hypothesis(h, gibson_qibla)
}
\seealso{
\code{\link[=qibla_hypothesis]{qibla_hypothesis()}}, \code{\link[=compare_qibla_hypotheses]{compare_qibla_hypotheses()}}
}
