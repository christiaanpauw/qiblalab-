% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{run_sensitivity_analysis}
\alias{run_sensitivity_analysis}
\title{Sensitivity analysis for a qibla hypothesis}
\usage{
run_sensitivity_analysis(
  data,
  hypothesis,
  mode = c("tolerance", "probabilistic"),
  tolerances = c(2, 5, 10),
  uncertainty = NULL,
  n_samples = 200L,
  model = bearing_haversine,
  seed = NULL,
  lat_col = "latitude",
  lon_col = "longitude",
  azimuth_col = "azimuth"
)
}
\arguments{
\item{data}{A mosque data frame.}

\item{hypothesis}{A \code{qibla_hypothesis} object.}

\item{mode}{\code{"tolerance"} or \code{"probabilistic"} (see Details).}

\item{tolerances}{Numeric vector of tolerance values in degrees. Default
\code{c(2, 5, 10)}.}

\item{uncertainty}{A \code{qibla_uncertainty} object. Required for
\code{mode = "probabilistic"}.}

\item{n_samples}{Integer. Monte Carlo draw count. Default \code{200}.}

\item{model}{Bearing model function. Default \code{\link[=bearing_haversine]{bearing_haversine()}}.}

\item{seed}{Integer seed for the Monte Carlo draws.}

\item{lat_col, lon_col, azimuth_col}{Column names in \code{data}.}
}
\value{
A tibble. For \code{mode = "tolerance"}: columns \code{tolerance},
\code{n_consistent}, \code{proportion}, \code{binom_p}. For \code{mode = "probabilistic"}:
\code{tolerance}, \code{proportion}, \code{sd_proportion}, \code{binom_p}, \code{sd_binom_p},
\code{n_samples}.
}
\description{
Evaluates a \code{\link[=qibla_hypothesis]{qibla_hypothesis()}} across a range of tolerances
(\code{mode = "tolerance"}) and optionally across Monte Carlo datasets drawn
from an uncertainty specification (\code{mode = "probabilistic"}).
}
\examples{
data(gibson_qibla)
h <- qibla_hypothesis("Test", candidate = "petra", tolerance = 5)
run_sensitivity_analysis(gibson_qibla, h)
}
\seealso{
\code{\link[=define_uncertainty]{define_uncertainty()}}, \code{\link[=sample_qibla_dataset]{sample_qibla_dataset()}},
\code{\link[=test_qibla_hypothesis]{test_qibla_hypothesis()}}
}
