% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{import_qibla_data}
\alias{import_qibla_data}
\title{Import the Early Islamic Qibla Database from a local XLSX file}
\usage{
import_qibla_data(path = NULL)
}
\arguments{
\item{path}{Character scalar. Path to \code{Mosques_Jan2021.xlsx}. If \code{NULL}
(default), calls \code{\link[=download_qibla_data]{download_qibla_data()}} to locate or fetch the file.}
}
\value{
A data frame with 160 rows and 13 columns (12 original + \code{row_id}).
Column names are exactly as in the source file. The \code{provenance} attribute
records the source file, DOI, MD5, and import timestamp.
}
\description{
Reads the raw Figshare spreadsheet and returns a data frame with all 12
original columns preserved plus an immutable \code{row_id}. No cleaning is
applied — call \code{\link[=validate_qibla_data]{validate_qibla_data()}} to surface anomalies and
\code{\link[=prepare_qibla_data]{prepare_qibla_data()}} to produce the analysis-ready form.
}
\examples{
\dontrun{
raw <- import_qibla_data()
names(raw)
}
}
\seealso{
\code{\link[=download_qibla_data]{download_qibla_data()}}, \code{\link[=validate_qibla_data]{validate_qibla_data()}}, \code{\link[=prepare_qibla_data]{prepare_qibla_data()}}
}
