% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{define_uncertainty}
\alias{define_uncertainty}
\title{Define an uncertainty specification for Monte Carlo sampling}
\usage{
define_uncertainty(coord_sigma = 0, date_sigma = 5, azimuth_sigma = 0)
}
\arguments{
\item{coord_sigma}{Numeric >= 0. 1-sigma positional uncertainty in decimal
degrees (Gaussian, applied independently to latitude and longitude).
Default \code{0} (no positional perturbation).}

\item{date_sigma}{Numeric >= 0. 1-sigma date uncertainty in years
(Gaussian). Applied to records where \code{year_ce_min == year_ce_max}
(nominally exact dates). Interval-dated records are handled by uniform
sampling inside \code{\link[=sample_qibla_dataset]{sample_qibla_dataset()}}. Default \code{5}.}

\item{azimuth_sigma}{Numeric >= 0. 1-sigma azimuth measurement uncertainty
in degrees (Gaussian, wrapped to [0, 360)). Default \code{0}.}
}
\value{
A \code{qibla_uncertainty} object.
}
\description{
Records assumed uncertainties for mosque coordinates, construction dates,
and azimuth measurements. Pass the result to \code{\link[=sample_qibla_dataset]{sample_qibla_dataset()}} or
\code{\link[=run_sensitivity_analysis]{run_sensitivity_analysis()}} with \code{mode = "probabilistic"}.
}
\examples{
u <- define_uncertainty(coord_sigma = 0.01, date_sigma = 10, azimuth_sigma = 2)
u
}
\seealso{
\code{\link[=sample_qibla_dataset]{sample_qibla_dataset()}}, \code{\link[=run_sensitivity_analysis]{run_sensitivity_analysis()}}
}
