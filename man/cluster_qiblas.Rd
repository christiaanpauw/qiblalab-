% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{cluster_qiblas}
\alias{cluster_qiblas}
\title{Cluster mosque orientations into directional groups}
\usage{
cluster_qiblas(
  data,
  k = 3L,
  method = c("circular_kmeans", "von_mises_mixture", "hierarchical"),
  azimuth_col = "azimuth",
  features = NULL,
  k_max = 8L,
  n_init = 10L,
  seed = NULL
)
}
\arguments{
\item{data}{A mosque data frame (e.g. \link{gibson_qibla}).}

\item{k}{Positive integer. Number of clusters.}

\item{method}{Clustering method (see Details).}

\item{azimuth_col}{Column containing observed azimuths. Default \code{"azimuth"}.}

\item{features}{\code{NULL} (cluster on \code{azimuth_col}) or a single character
column name to cluster on instead (must not be a forbidden column).}

\item{k_max}{Maximum k for the model selection scan. Default \code{8}.}

\item{n_init}{Number of random starts for \code{"circular_kmeans"}. Default \code{10}.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{qibla_cluster_result} object. Key fields:
\describe{
\item{\code{assignments}}{Integer vector (one per row of \code{data}). Cluster label,
or \code{NA} for mosques without an azimuth.}
\item{\code{centroids}}{Tibble with \code{cluster}, \code{direction_deg}, \code{direction_rad},
\code{n_members}, \code{dispersion} (1 − R̄ per cluster).}
\item{\code{model_selection}}{Tibble with \code{k}, \code{criterion}, \code{value} for k =
1..\code{k_max}.}
}
}
\description{
Applies a circular clustering method to the observed azimuth column of a
mosque data frame. Three methods are available:
\describe{
\item{\code{circular_kmeans}}{Built-in circular k-means using the chord
dissimilarity 1 − cos(θ). Multiple random starts are used.}
\item{\code{von_mises_mixture}}{von Mises–Fisher mixture model via
\code{movMF::movMF()}. Requires the \code{movMF} package (in Suggests).}
\item{\code{hierarchical}}{Agglomerative hierarchical clustering with angular
distance and Ward linkage.}
}
}
\details{
The function always fits k = 1 through \code{k_max} and attaches a model
selection table to the output (ADR-09). Cluster assignments are entirely
destination-blind: \code{gibson_classification} and \code{gibson_classification_source}
are forbidden as clustering features (ADR-07).
}
\examples{
data(gibson_qibla)
res <- cluster_qiblas(gibson_qibla, k = 3, seed = 1)
print(res)
}
\seealso{
\code{\link[=validate_clusters]{validate_clusters()}}
}
