% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{sample_qibla_dataset}
\alias{sample_qibla_dataset}
\title{Draw one Monte Carlo sample from an uncertainty specification}
\usage{
sample_qibla_dataset(data, uncertainty, seed = NULL)
}
\arguments{
\item{data}{A mosque data frame (typically \link{gibson_qibla}).}

\item{uncertainty}{A \code{qibla_uncertainty} object from \code{\link[=define_uncertainty]{define_uncertainty()}}.}

\item{seed}{Optional integer seed for reproducibility of a single draw.}
}
\value{
A tibble with the same columns as \code{data}, with selected values
perturbed.
}
\description{
Returns a copy of \code{data} with coordinates, dates, and azimuths perturbed
according to a \code{\link[=define_uncertainty]{define_uncertainty()}} specification. Call this repeatedly
inside a Monte Carlo loop. NA values are never moved: mosques without
measured azimuths remain without azimuths after sampling; only the numeric
values are perturbed.
}
\details{
Date handling (ADR-04):
\itemize{
\item Exact dates (\code{year_ce_min == year_ce_max}): drawn from
Normal(year_ce_min, date_sigma), rounded to the nearest year.
\item Interval dates (\code{year_ce_min < year_ce_max}): drawn uniformly from
[year_ce_min, year_ce_max], rounded to the nearest year.
\item Unknown dates (NA): remain NA.
}
The original \code{year_ce} character column is not modified.
}
\examples{
data(gibson_qibla)
u <- define_uncertainty(coord_sigma = 0.01, azimuth_sigma = 2)
s <- sample_qibla_dataset(gibson_qibla, u, seed = 1)
head(s[, c("latitude", "longitude", "azimuth")])
}
\seealso{
\code{\link[=define_uncertainty]{define_uncertainty()}}, \code{\link[=run_sensitivity_analysis]{run_sensitivity_analysis()}}
}
