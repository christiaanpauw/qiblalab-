% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster-validation.R
\name{validate_clusters}
\alias{validate_clusters}
\title{Validate a cluster solution across internal, stability, and external criteria}
\usage{
validate_clusters(
  result,
  external = NULL,
  bootstrap_n = 100L,
  predictive_splits = 20L,
  seed = NULL
)
}
\arguments{
\item{result}{A \code{qibla_cluster_result} from \code{\link[=cluster_qiblas]{cluster_qiblas()}}.}

\item{external}{Character vector of column names to use as external labels.
May include \code{"gibson_classification"} (post-hoc only). Default \code{NULL}
(skip external validation).}

\item{bootstrap_n}{Number of bootstrap resamples for stability. Default 100.}

\item{predictive_splits}{Number of train/test splits. Default 20.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{qibla_cluster_validation} object. Print it for a structured
report with pass / warn / fail indicators.
}
\description{
Evaluates a \code{\link[=cluster_qiblas]{cluster_qiblas()}} result against the three mandatory validation
sections specified in ADR-10:
}
\details{
\enumerate{
\item \strong{Internal}: within-cluster circular dispersion, minimum centroid
separation, and (for \code{von_mises_mixture}) proportion of ambiguous soft
assignments.
\item \strong{Stability}: bootstrap adjusted Rand index (ARI) across \code{bootstrap_n}
resamples. Mean ARI > 0.6 is required to claim stability (ADR-10).
\item \strong{External}: ARI, normalised mutual information (NMI), and cluster
purity against columns you supply. Gibson's classification may only
appear here — never in the clustering input — and is labelled
"external validation against Gibson's classification" in all output.
\item \strong{Predictive} (when n ≥ 60): mean angular prediction error on held-out
mosques across \code{predictive_splits} random 80/20 train-test splits.
}
}
\examples{
data(gibson_qibla)
res <- cluster_qiblas(gibson_qibla, k = 3, seed = 1)
val <- validate_clusters(res, external = c("age_group", "country"),
                          bootstrap_n = 20L, seed = 2)
print(val)
}
\seealso{
\code{\link[=cluster_qiblas]{cluster_qiblas()}}
}
