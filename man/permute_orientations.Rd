% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{permute_orientations}
\alias{permute_orientations}
\title{Permute azimuth values in a mosque dataset}
\usage{
permute_orientations(
  data,
  azimuth_col = "azimuth",
  block_col = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{A mosque data frame.}

\item{azimuth_col}{Column containing observed azimuths. Default \code{"azimuth"}.}

\item{block_col}{Optional character column name. If supplied, azimuths are
permuted within the levels of this column (e.g. \code{"country"} for within-
country permutation).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble with the same structure as \code{data}, \code{azimuth_col} shuffled.
}
\description{
Returns a copy of \code{data} with the azimuth column randomly shuffled,
breaking any association between observed orientations and geographic,
temporal, or classification variables. \code{NA} positions are held fixed: only
the non-\code{NA} azimuth values are permuted.
}
\details{
When \code{block_col} is supplied, permutation is performed within each level
of that column independently (block permutation). This preserves the
geographic distribution of azimuths within regions while breaking
any region-level systematic association.
}
\examples{
data(gibson_qibla)
shuffled <- permute_orientations(gibson_qibla, seed = 1)
# The set of non-NA azimuths is preserved; their assignment to mosques is not.
all.equal(sort(gibson_qibla$azimuth, na.last = NA),
          sort(shuffled$azimuth,    na.last = NA))
}
\seealso{
\code{\link[=permutation_test]{permutation_test()}}
}
