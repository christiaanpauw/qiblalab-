% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{permutation_test}
\alias{permutation_test}
\title{Permutation test for mosque orientation statistics}
\usage{
permutation_test(
  data,
  stat_fn,
  n_permutations = 999L,
  alternative = c("greater", "less", "two.sided"),
  azimuth_col = "azimuth",
  block_col = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{A mosque data frame.}

\item{stat_fn}{A function \code{f(data)} returning a single numeric scalar. This
is the test statistic.}

\item{n_permutations}{Positive integer. Number of permutation replicates.
Default \code{999}.}

\item{alternative}{\code{"greater"}, \code{"less"}, or \code{"two.sided"}. Direction of
the permutation p-value. Default \code{"greater"}.}

\item{azimuth_col}{Column to permute. Default \code{"azimuth"}.}

\item{block_col}{Optional: permute within the levels of this column.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{qibla_permutation_test} object. Key fields:
\describe{
\item{\code{observed}}{The test statistic on the original data.}
\item{\code{permuted}}{Numeric vector of length <= \code{n_permutations}.}
\item{\code{p.value}}{Permutation p-value.}
\item{\code{alternative}, \code{n_permutations}}{As supplied.}
}
}
\description{
Evaluates a user-supplied test statistic under a permutation null
distribution. The null hypothesis is that the observed azimuth values are
exchangeable across mosques — i.e., any assignment of azimuths to mosque
locations is equally likely. The p-value is the proportion of permuted
replicates at least as extreme as the observed value.
}
\details{
The statistic function must return a single numeric scalar. If it returns
\code{NA} for permuted data (e.g. because the population filter yields no rows),
those replicates are excluded with a warning.
}
\examples{
data(gibson_qibla)
data(destinations)
mecca <- destinations[destinations$id == "mecca", ]

# Stat: mean resultant length of residuals (high = concentrated toward Mecca)
stat <- function(d) {
  b   <- bearing_to_destination(d, mecca)
  err <- absolute_angular_error(d$azimuth, b)
  circular_resultant(err[!is.na(err)])
}
\dontrun{
res <- permutation_test(gibson_qibla, stat, n_permutations = 99, seed = 1)
print(res)
}
}
\seealso{
\code{\link[=permute_orientations]{permute_orientations()}}
}
