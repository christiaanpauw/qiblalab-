% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular-stats.R
\name{circular_mean}
\alias{circular_mean}
\title{Circular (directional) mean}
\usage{
circular_mean(x, na.rm = FALSE)
}
\arguments{
\item{x}{Numeric vector of angles in degrees.}

\item{na.rm}{Logical. Remove \code{NA} values before computing. Default \code{FALSE}.}
}
\value{
A single numeric value in [0, 360), or \code{NA} if \code{x} is empty or all
\code{NA}.
}
\description{
Returns the mean direction of a set of angles, computed as the argument of
the mean unit vector. Correctly handles wrap-around (e.g. the mean of 1 deg
and 359 deg is 0 deg, not 180 deg).
}
\examples{
circular_mean(c(10, 350))   # 0, not 180
circular_mean(c(90, 180, 270, 0))
}
